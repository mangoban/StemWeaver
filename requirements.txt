# Core audio processing
numpy
soundfile
librosa
scipy

# PyTorch (CPU only)
torch==2.9.1+cpu
torchaudio==2.9.1+cpu

# Demucs AI models
demucs==4.0.1

# GUI
dearpygui==2.1.1

# Remove heavy dependencies
# llvmlite (numba dependency - 160MB)
# numba (not needed for basic audio processing)